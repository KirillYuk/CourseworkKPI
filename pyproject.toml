[project]
name = "financial-data-monitor"
version = "0.1.0"
authors = [
    { name = "Kyrylo Yukhymchuk" }
]
requires-python = ">=3.12"

dependencies = [
    "rich",
    "requests",
    "python-dotenv"
]

[tool.setuptools.packages.find]
where = ["src"]